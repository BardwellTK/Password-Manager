namespace Converter
{
    class Entity
    {
        public string title, data;
        public Entity(string inTitle, string inData)
        {
            title = inTitle;
            data = inData;
        }
    }
}
