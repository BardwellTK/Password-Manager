using System;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.IO;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Converter
{
    class Compare
    {
        public Compare()
        {
        }

        public List<Container> ListCompare(List<Container> desktop, List<Container> laptop)
        {
            //Both containers will have an index of 2, so [0] and [1]
            //[0] == Favourites
            //[1] == General

            foreach (Box b in desktop)
            {
                
            }
        }
    }
}
